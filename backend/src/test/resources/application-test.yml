spring:
  datasource:
    url: jdbc:h2:mem:asset_manager_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password:

  flyway:
    enabled: true

jwt:
  secret: test-secret-key-at-least-32-bytes-long
  expiration: 604800
